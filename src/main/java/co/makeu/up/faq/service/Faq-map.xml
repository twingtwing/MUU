<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="co.makeu.up.faq.service.FaqMapper">
	<select id="faqSelectList" resultType="co.makeu.up.faq.service.FaqVO">
		SELECT * FROM FAQ
		WHERE F_ST_CODE = 'F01'
		ORDER BY F_NO
	</select>
	
	<select id="faqList" resultType="co.makeu.up.faq.service.FaqVO">
		SELECT F_NO, CT.CODE_NAME CTGR, Q_CONTENT, C.CODE_NAME F_ST_CODE, TO_CHAR(WR_DATE,'YYYY-MM-DD') WR_DATE,A_CONTENT 
		FROM FAQ F JOIN COMM_CODE C
		ON F.F_ST_CODE = C.CODE_ID
		JOIN COMM_CODE CT
		ON F.CTGR = CT.CODE_ID
		ORDER BY F_NO
	</select>
</mapper>